@page "/NewTransaction"
@inject IAccountService AccountService
@inject AppState AppState
@inject NavigationManager Navigation

<!-- Transaction page -->
<h3 class="mb-3">Transaction</h3>
@if (!AppState.IsLoggedIn)
{
    <!-- Not loged in -->
    <p class="text-danger">You are not logged in, rederecting</p>
}
else if (_accounts.Count < 2)
{
    <!-- Not enough accounts to make a transaction -->
    <p>Create at least 2 accounts to make a transaction</p>
}
else
{
    <!-- Transaction form -->
    <EditForm Model="_model" OnValidSubmit="OnSubmitAsync">
        <DataAnnotationsValidator />
        <ValidationSummary class="text-danger mb-3" />

        <!-- Transfer from -->
        <div class="mb-3">
            <label class="form-label">Transfer from</label>
            <InputSelect TValue="Guid"
                         @bind-Value="_model.FromAccountId"
                         class="form-select"
                         @onchange="HandleFormChanged">
                <option value="@Guid.Empty">Choose Account</option>
                @foreach (var account in _accounts)
                {
                    <option value="@account.Id">@account.Name (@account.Balance @account.Currency)</option>
                }
            </InputSelect>
        </div>

        <!-- Transfer to -->
        <div class="mb-3">
            <label class="form-label">Transfer to</label>
            <InputSelect TValue="Guid"
                         @bind-Value="_model.ToAccountId"
                         class="form-select"
                         disabled="@(_model.FromAccountId == Guid.Empty)">
                <option value="@Guid.Empty">Choose Account</option>
                @foreach (var account in _toAccounts.Where(x => x.Id != _model.FromAccountId))
                {
                    <option value="@account.Id">@account.Name (@account.Balance @account.Currency)</option>
                }
            </InputSelect>
        </div>

        <!-- Amount -->
        <div class="mb-3">
            <label class="form-label">Amount</label>
            <InputNumber TValue="decimal" @bind-Value="_model.Amount" class="form-control" min="0.01" step="0.01" />
        </div>

        <!-- Submit button -->
        <div class="mb-3">
            <button type="submit" class="btn btn-primary ">Complete Transaction</button>
        </div>
    </EditForm>
    @if (!string.IsNullOrEmpty(_transferError))
    {
        <p class="text-danger">@_transferError</p>
    }


    <!-- Withdraw and deposit -->
    <h3 class="mt-4 mb-3">Withdraw & Deposit</h3>
    @if (_accounts.Count < 1)
    {
        <p>Create at least one account to make a transaction</p>
    }
    else
    {
        <!-- Withdraw -->
        <div class="row">
            <div class="col-md-6">
                <div class="card shadow-sm p-4 mb-4" style="max-width: 400px;">
                    <h5 class="mb-3">Withdraw</h5>
                    <EditForm Model="_withdrawModel" OnValidSubmit="OnWithdrawAsync">
                        <DataAnnotationsValidator />
                        <ValidationSummary class="text-danger mb-3" />

                        <!-- Withdraw from-->
                        <div class="mb-3">
                            <label class="form-label">Withdraw from</label>
                            <InputSelect TValue="Guid"
                                         @bind-Value="_withdrawModel.FromAccountId"
                                         class="form-select"
                                         @onchange="HandleFormChanged">
                                <option value="@Guid.Empty">Choose Account</option>
                                @foreach (var account in _accounts)
                                {
                                    <option value="@account.Id">@account.Name (@account.Balance.ToString("N2") @account.Currency)</option>
                                }
                            </InputSelect>
                        </div>

                        <!-- Withdraw amount -->
                        <div class="mb-3">
                            <label class="form-label">Amount</label>
                            <InputNumber TValue="decimal" @bind-Value="_withdrawModel.Amount" class="form-control" min="0.01" step="0.01" />
                        </div>
                        <button type="submit" class="btn btn-primary w-100">Complete Withdrawal</button>
                    </EditForm>
                    @if (!string.IsNullOrEmpty(_withdrawError))
                    {
                        <p class="text-danger">@_withdrawError</p>
                    }
                </div>
            </div>

            <!-- Deposit -->
            <div class="col-md-6">
                <div class="card shadow-sm p-4 mb-4" style="max-width: 400px;">
                    <h5 class="mb-3">Deposit</h5>
                    <EditForm Model="_depositModel" OnValidSubmit="OnDepositAsync">
                        <DataAnnotationsValidator />
                        <ValidationSummary class="text-danger mb-3" />

                        <!-- Deposit to -->
                        <div class="mb-3">
                            <label class="form-label">Deposit to</label>
                            <InputSelect TValue="Guid"
                                         @bind-Value="_depositModel.ToAccountId"
                                         class="form-select"
                                         @onchange="HandleFormChanged">
                                <option value="@Guid.Empty">Choose Account</option>
                                @foreach (var account in _accounts)
                                {
                                    <option value="@account.Id">@account.Name (@account.Balance.ToString("N2") @account.Currency)</option>
                                }
                            </InputSelect>
                        </div>

                        <!-- Deposit amount -->
                        <div class="mb-3">
                            <label class="form-label">Amount</label>
                            <InputNumber TValue="decimal" @bind-Value="_depositModel.Amount" class="form-control" min="0.01" step="0.01" />
                        </div>
                        <button type="submit" class="btn btn-primary w-100">Complete Deposit</button>
                    </EditForm>
                    @if (!string.IsNullOrEmpty(_depositError))
                    {
                        <p class="text-danger">@_depositError</p>
                    }
                </div>
            </div>
        </div>
    }

    <!-- Account overwiev -->
    <div>
        <h3 class="mb-3">All accounts</h3>
        <ul class="list-group">
            @foreach (var account in _accounts)
            {
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <div>
                        <strong>@account.Name</strong> - @account.AccountType - @account.Balance - @account.Currency
                    </div>
                </li>
            }
        </ul>
    </div>
}

<!-- Coding stuff -->
@code
{
    //Private instance variables
    private string? _transferError;
    private string? _withdrawError;
    private string? _depositError;


    // Private lists and models
    private readonly TransferFormModel _model = new();
    private List<BankAccount> _accounts = new();
    private List<BankAccount> _toAccounts = new();
    private readonly WithdrawModel _withdrawModel = new();
    private readonly DepositModel _depositModel = new();

    /// <summary>
    /// Represent the transfer form field
    /// </summary>
    private class TransferFormModel
    {
        public Guid FromAccountId { get; set; }
        public Guid ToAccountId { get; set; }
        public decimal Amount { get; set; }
    }

    /// <summary>
    /// Model for the deposit transaction
    /// </summary>
    private class DepositModel
    {
        public Guid ToAccountId { get; set; }
        public decimal Amount { get; set; }
    }

    /// <summary>
    /// Model for the withdrawal transaction
    /// </summary>
    private class WithdrawModel
    {
        public Guid FromAccountId { get; set; }
        public decimal Amount { get; set; }
    }

    /// <summary>
    /// Loads all account when the component initializes
    /// </summary>
    protected override async Task OnInitializedAsync()
    {
        // Inloggningskontroll
        if (!AppState.IsLoggedIn)
        {
            Console.WriteLine("[NewTransaction] User not logged in — redirecting to /home.");
            Navigation.NavigateTo("/home", replace: true);
            return;
        }
        Console.WriteLine("[NewTransaction] Loading accounts...");
        await AccountService.EnsureLoadedAsync();
        _accounts = AccountService.GetAccounts();
        _toAccounts = _accounts;
        Console.WriteLine($"[NewTransaction] {_accounts.Count} accounts loaded.");
    }

    /// <summary>
    /// Handels form submission for completing a transfer between two accounts
    /// Includes validation and refreshes the account list
    /// </summary>
    private async Task OnSubmitAsync()
    {
        _transferError = null;
        if (_model.Amount <= 0)
        {
            _transferError = "Not allowed: Transfer amount must be greater than 0.";
            return;
        }
        Console.WriteLine("[NewTransaction] Transfer initiated.");
        if (_model.FromAccountId == Guid.Empty || _model.ToAccountId == Guid.Empty) //Checks if both account selcted are not the same
        {
            Console.WriteLine("[NewTransaction] Validation failed: Missing source or destination account.");
            StateHasChanged();
        }
        if (_model.FromAccountId == _model.ToAccountId)
        {
            Console.WriteLine("[NewTransaction] Validation failed: Cannot transfer to the same account.");
            StateHasChanged();
        }
        try
        {
            Console.WriteLine($"[NewTransaction] Transferring {_model.Amount} from {_model.FromAccountId} → {_model.ToAccountId}");
            await AccountService.Transfer(_model.FromAccountId, _model.ToAccountId, _model.Amount); //Perform transfer
            _accounts = AccountService.GetAccounts();   //Updating list

            //Show confirmation
            var from = _accounts.First(x => x.Id == _model.FromAccountId);
            var to = _accounts.First(x => x.Id == _model.ToAccountId);
            _model.Amount = 0;
            Console.WriteLine("[NewTransaction] Transfer successful. Account list refreshed.");
            StateHasChanged();
        }
        catch (Exception ex)
        {
            _transferError = $"Not allowed: {ex.Message}";
        }
    }

    /// <summary>
    /// Parses the new value and updates available "To" account
    /// </summary>
    private void HandleFormChanged(ChangeEventArgs eventArgs)
    {
        if (Guid.TryParse(eventArgs.Value?.ToString(), out var id))
        {
            OnFormChanged(id);
        }
    }

    /// <summary>
    /// Uppdates the "To" account list based on the "From" account.
    /// Prevents selecting the same account 
    /// </summary>
    /// <param name="fromId">The selected account ID</param>
    private void OnFormChanged(Guid fromId)
    {
        _model.FromAccountId = fromId;
        var from = _accounts.FirstOrDefault(x => x.Id == fromId);
        if (from != null)
        {
            _toAccounts = _accounts.Where(account => account.Id != fromId).ToList();
        }
        else
        {
            _toAccounts = _accounts.ToList();
            Console.WriteLine("[NewTransaction] No 'From' account selected. Reset 'To' list.");
        }
        _model.ToAccountId = Guid.Empty;
    }

    /// <summary>
    /// Handles a withdrawl trasaction
    /// </summary>
    private async Task OnWithdrawAsync()
    {
        _withdrawError = null;
        if (_withdrawModel.Amount <= 0)
        {
            _withdrawError = "Not allowed: Withdrawal amount must be greater than 0.";
            return;
        }
        try
        {
            Console.WriteLine($"[NewTransaction] Withdrawal requested: {_withdrawModel.Amount} from {_withdrawModel.FromAccountId}");
            await AccountService.WithdrawAsync(_withdrawModel.FromAccountId, _withdrawModel.Amount);
            _accounts = AccountService.GetAccounts();
            _withdrawModel.Amount = 0;
            Console.WriteLine("[NewTransaction] Withdrawal successful. Account list refreshed.");
            StateHasChanged();
        }
        catch (Exception ex)
        {
            _withdrawError = $"Not allowed: {ex.Message}";
        }
    }

    /// <summary>
    /// Handles a deposit transaction
    /// </summary>
    private async Task OnDepositAsync()
    {
        _depositError = null;
        if (_depositModel.Amount <= 0)
        {
            _depositError = "Not allowed: Deposit amount must be greater than 0.";
            return;
        }
        try
        {
            Console.WriteLine($"[NewTransaction] Deposit requested: {_depositModel.Amount} from {_depositModel.ToAccountId}");
            await AccountService.DepositAsync(_depositModel.ToAccountId, _depositModel.Amount);
            _accounts = AccountService.GetAccounts();
            _depositModel.Amount = 0;
            Console.WriteLine("[NewTransaction] Deposit successful. Account list refreshed.");
            StateHasChanged();
        }
        catch (Exception ex)
        {
            _depositError = $"Not allowed: {ex.Message}";
        }
    }
}

